# ============================================================
# RIME — Python Dependencies
# Pin exact versions to ensure reproducible builds.
# ============================================================

# --- Web framework & realtime transport ---
fastapi==0.111.0
uvicorn[standard]==0.29.0
websockets==12.0
python-multipart==0.0.9
starlette==0.37.2

# --- Audio processing ---
pyaudio==0.2.14
numpy==1.26.4
sounddevice==0.4.7

# --- VAD ---
silero-vad==5.1
torch==2.3.0
torchaudio==2.3.0

# --- STT (Whisper) ---
openai-whisper==20240930
faster-whisper==1.0.3

# --- Speaker embeddings ---
speechbrain==1.0.0

# --- LLM (local Ollama client) ---
ollama==0.2.1
openai==1.30.1          # external LLM fallback only, via ExternalServiceManager

# --- Database ---
aiosqlite==0.20.0

# --- HTTP client (for external services) ---
httpx==0.27.0
aiohttp==3.9.5

# --- Utilities ---
python-dotenv==1.0.1
pydantic==2.7.1
pydantic-settings==2.2.1
uuid7==0.1.0
python-jose[cryptography]==3.3.0
structlog==24.2.0
rich==13.7.1

# --- Testing ---
pytest==8.2.2
pytest-asyncio==0.23.7
pytest-cov==5.0.0
httpx==0.27.0            # async test client for FastAPI

# --- Dev tools ---
black==24.4.2
ruff==0.4.7
